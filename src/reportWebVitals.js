// reportWebVitals.js
import { reportWebVitals } from 'web-vitals';

const reportWebVitalsCallback = (metric) => {
  console.log(metric);  // Optionally log the metrics to the console.
};

// Default export of the reportWebVitals function
export default reportWebVitalsCallback;
